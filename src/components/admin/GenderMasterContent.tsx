import LookupMasterContent from "./shared/LookupMasterContent";
export default function GenderMasterContent() {
  return (
    <LookupMasterContent
      tableName="genders"
      title="Genders / लिंग"
      description="Options in the Gender dropdown on Student Registration."
    />
  );
}